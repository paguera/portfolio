# Backend API endpoint URL (including '/api' if direct, e.g. http://localhost:3000/api)
# In production with reverse proxy routing /api, this can be set to '/api' or your API domain
VITE_BASE_URL=http://localhost:3000/api
